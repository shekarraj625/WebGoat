// ❌ VULNERABLE: Hardcoded credentials
public class AuthService {
    private static final String API_KEY = "12345-SECRET-KEY"; // Hardcoded secret

    public boolean validate(String key) {
        return key.equals(API_KEY);
    }
}
// ❌ VULNERABLE: XSS
protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws IOException {
    String name = req.getParameter("name"); // Attacker can send <script>alert(1)</script>

    resp.getWriter().println("Hello " + name); // Reflects raw input → XSS
}
// ❌ VULNERABLE: Added to Vulnerabilities 
